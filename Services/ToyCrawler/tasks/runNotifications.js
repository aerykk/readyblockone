// Check for arbitrage using user's destinations

// If this product has a large variance in 2 regions, 
//   check for users who have these regions in their settings
//   if they do, then notify them of this product's arbitrage eligibility
// Match the percent difference saved in the list against the user's settings,
//   so we don't bother them with unprofitable suggestions

        var a = function() {
            // runBennyCheck();
            // runR2D2Check();
            // runPetShopCheck();

            setTimeout(a, 2 * 60 * 1000); // run once every 2 mins
        };

        a();

        var d = function() {
            // runRedFlagDealsCheck();
            // runCanadaBrickPickerCheck();

            setTimeout(d, 2 * 60 * 1000); // run once every 2 mins
        };

        d();



        var runOtherRetailers = function() {